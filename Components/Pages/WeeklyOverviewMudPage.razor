@page "/weekly-reports/mud"
@attribute [Authorize(Policy = "ConfirmedEmail")]
@inject IStringLocalizer<SharedResource> Localizer

<PageTitle>@Localizer["WeeklyOverviewMudPageTitle"]</PageTitle>

@if (Overview is null)
{
    <MudProgressLinear Color="Color.Primary" Indeterminate="true" />
}
else
{
    <MudContainer MaxWidth="MaxWidth.False" Class="weekly-mud-page">
        <MudPaper Elevation="0" Outlined="true" Class="weekly-mud-hero">
            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Class="weekly-mud-hero-stack">
                <MudStack Spacing="1">
                    <MudText Typo="Typo.overline" Color="Color.Primary">@Localizer["WeeklyOverviewEyebrow"]</MudText>
                    <MudText Typo="Typo.h4">@Localizer["WeeklyOverviewMudPageTitle"]</MudText>
                    <MudText Typo="Typo.body2" Color="Color.Secondary">@Localizer["WeeklyOverviewPageIntro"]</MudText>
                </MudStack>

                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="weekly-mud-controls">
                    <MudButton ButtonType="ButtonType.Button" Variant="Variant.Outlined" Color="Color.Primary" Size="Size.Small" OnClick="GoToToday">@Localizer["ReportEntryToday"]</MudButton>
                    <MudIconButton Icon="@Icons.Material.Filled.ChevronLeft" Size="Size.Small" Color="Color.Default" OnClick="GoToPreviousWeek" aria-label="@Localizer["WeeklyOverviewPreviousWeek"]" />
                    <MudDatePicker Date="SelectedDate" DateChanged="HandleDateChangedAsync" DateFormat="dd.MM.yyyy" Margin="Margin.Dense" Variant="Variant.Outlined" Class="weekly-mud-date-picker" />
                    <MudIconButton Icon="@Icons.Material.Filled.ChevronRight" Size="Size.Small" Color="Color.Default" OnClick="GoToNextWeek" aria-label="@Localizer["WeeklyOverviewNextWeek"]" />
                    <MudButton Href="@ExportHref" Target="_self" Variant="Variant.Filled" Color="Color.Primary" Size="Size.Small" StartIcon="@Icons.Material.Filled.PictureAsPdf">@Localizer["DashboardExportPdf"]</MudButton>
                </MudStack>
            </MudStack>
        </MudPaper>

        <MudGrid Spacing="3" Class="weekly-mud-metrics">
            <MudItem xs="12" md="4">
                <MudPaper Elevation="0" Outlined="true" Class="weekly-mud-metric">
                    <MudText Typo="Typo.caption" Color="Color.Secondary">@Localizer["WeeklyOverviewTitle"]</MudText>
                    <MudText Typo="Typo.h5">@Overview.CalendarWeek</MudText>
                </MudPaper>
            </MudItem>
            <MudItem xs="12" md="4">
                <MudPaper Elevation="0" Outlined="true" Class="weekly-mud-metric">
                    <MudText Typo="Typo.caption" Color="Color.Secondary">@Localizer["WeeklyOverviewRecorded"]</MudText>
                    <MudText Typo="Typo.h5">@Overview.RecordedHours.ToString("0.##") h</MudText>
                </MudPaper>
            </MudItem>
            <MudItem xs="12" md="4">
                <MudPaper Elevation="0" Outlined="true" Class="weekly-mud-metric">
                    <MudText Typo="Typo.caption" Color="Color.Secondary">@Localizer["DashboardRemainingHours"]</MudText>
                    <MudText Typo="Typo.h5">@Overview.RemainingHours.ToString("0.##") h</MudText>
                </MudPaper>
            </MudItem>
        </MudGrid>

        <MudGrid Spacing="3" Class="weekly-mud-days">
            @foreach (var day in Overview.Days)
            {
                <MudItem xs="12" md="6" xl="2">
                    <MudCard Elevation="0" Outlined="true" Class="weekly-mud-day-card">
                        <MudCardHeader>
                            <CardHeaderContent>
                                <MudLink Href="@($"report-entries/new?date={day.Date:yyyy-MM-dd}")" Typo="Typo.h6" Class="weekly-mud-day-title">@Localizer[day.DayKey]</MudLink>
                                <MudText Typo="Typo.body2" Color="Color.Secondary">@day.Date.ToString("dd.MM.yyyy")</MudText>
                            </CardHeaderContent>
                            <CardHeaderActions>
                                <MudChip T="string" Color="Color.Primary" Variant="Variant.Filled" Size="Size.Small">@day.TotalHours.ToString("0.##") h</MudChip>
                            </CardHeaderActions>
                        </MudCardHeader>
                        <MudCardContent>
                            <MudText Typo="Typo.caption" Color="Color.Secondary">@day.EntryCount @Localizer["WeeklyOverviewEntries"]</MudText>

                            @if (day.Entries.Count == 0)
                            {
                                <MudPaper Elevation="0" Class="weekly-mud-empty">
                                    <MudText Typo="Typo.body2" Color="Color.Secondary">@Localizer["WeeklyOverviewNoEntries"]</MudText>
                                </MudPaper>
                            }
                            else
                            {
                                <MudStack Spacing="1" Class="weekly-mud-entry-list">
                                    @foreach (var entry in day.Entries)
                                    {
                                        <MudPaper Elevation="0" Outlined="true" Class="weekly-mud-entry">
                                            <MudText Typo="Typo.caption" Color="Color.Secondary">@entry.TimeRange</MudText>
                                            <MudLink Href="@($"report-entries/{entry.Id}")" Typo="Typo.subtitle2" Class="weekly-mud-entry-title">@entry.Title</MudLink>
                                            <MudStack Row="true" Spacing="1" Class="weekly-mud-entry-meta">
                                                <MudChip T="string" Size="Size.Small" Variant="Variant.Outlined">@entry.CategoryName</MudChip>
                                                @if (!string.IsNullOrWhiteSpace(entry.OrderNumber))
                                                {
                                                    <MudChip T="string" Size="Size.Small" Color="Color.Info" Variant="Variant.Outlined">@entry.OrderNumber</MudChip>
                                                }
                                            </MudStack>
                                        </MudPaper>
                                    }
                                </MudStack>
                            }
                        </MudCardContent>
                    </MudCard>
                </MudItem>
            }
        </MudGrid>
    </MudContainer>
}
